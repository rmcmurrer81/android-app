# Sarah Travel OS 2.3 public release

This branch contains the complete public Android and Windows source package. GitHub Actions reconstructs the source archive, verifies SHA-256, materializes readable source, runs tests, builds the Android APK, and builds one self-installing Windows EXE. The release is not accepted merely because files exist; both workflows and artifact checks must pass.
