# Sarah phone fix build

This branch keeps the uploaded v0.3 package and adds an active fixed build.

Fixes in version 0.3.1-phone-fix:

- onboarding instructions no longer depend on long clipped hints;
- clearer labeled fields and card styling;
- chat composer kept above Android navigation and keyboard areas;
- message field made visibly editable;
- Android text-to-speech now queues Sarah's first greeting until the phone speech engine is ready;
- versionCode increased to 3.

The earlier state remains recoverable in Git history before these commits.
